# Runtime dependencies for the CIS Stockroom Inventory System.
# Deliberately small -- this runs on a Raspberry Pi.
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
python-barcode>=0.15
# Downscales uploaded item photos. A phone photo is 3-5 MB and the SD
# card is this system's weakest component; apt has it as python3-pil.
Pillow>=10
